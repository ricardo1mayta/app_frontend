import { PintarbolillaPipe } from './pintarbolilla.pipe';

describe('PintarbolillaPipe', () => {
  it('create an instance', () => {
    const pipe = new PintarbolillaPipe();
    expect(pipe).toBeTruthy();
  });
});
