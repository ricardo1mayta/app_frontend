import { SerieparentPipe } from './serieparent.pipe';

describe('SerieparentPipe', () => {
  it('create an instance', () => {
    const pipe = new SerieparentPipe();
    expect(pipe).toBeTruthy();
  });
});
